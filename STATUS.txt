YOUNIQUE — CURRENT STATUS
=========================
Dynamisch document. Bijwerken bij elke commit en aan het einde van elke sessie.
Voor architectuur en conventies: zie PROJECT.txt.

LAATST BIJGEWERKT
-----------------
2026-06-27  (sessie: home section headers boven cards + gecentreerd)

BRANCH STATE
------------
Branch: main
2 commits ahead of origin/main — nog niet gepushed.

RECENT COMMITS (top 6)
----------------------
ccc0c76  Lift home section headers above cards and center them
4782578  Mark branch as in sync with origin
48e9d64  Mark share-message key as manually managed
281e559  Drop duplicate ShareLink message parameter
4aa0b56  Record icon commit SHA and refresh extraction state
7534f4a  Add dark and tinted app icon variants

WAT IS RECENT GEBEURD
---------------------
- Home controlsSection: titels van de drie blokken (baseSettings,
  soundStyle, advanced) staan nu BOVEN de cards ipv erin, gecentreerd
  (frame(maxWidth: .infinity, alignment: .center) op de Text).
  Voor advancedSection betekent dit: de knop is nu alleen de summary +
  badge + chevron rij, de "Geavanceerd aanpassen" titel zit boven de knop.
  accessibilityLabel op de knop blijft "Geavanceerd aanpassen".
- "Wat vind je van de naam %@?" key in xcstrings: extractionState gezet op
  "manual" zodat Xcode hem niet bij elke build opnieuw als stale markeert.
  Het runtime gebruik (Bundle.appLocalizedString) is voor de extractor
  onzichtbaar; "manual" is Apple's signaal dat de key handmatig beheerd
  wordt.
- Bug-fix vervolg op d0fddff: in de share sheet stond de zin "Wat vind
  je van de naam X?" twee keer in het bericht (Messages, Mail). Oorzaak:
  ShareLink kreeg dezelfde tekst mee als `item:` én `message:` — sommige
  share-targets plakken ze achter elkaar. `message:` parameter weggehaald
  op beide sites (ContentView.swift:701 en FavoriteDetailView.swift:111).
  `subject:` blijft voor de mail-onderwerpregel.
- App icon polish voor iOS 18+ Liquid Glass appearances. Bron Y.png
  (cream/peach gradient + dark Y) was opaque met alpha-kanaal en de dark+
  tinted slots in Contents.json waren lege stubs. Nu:
  - Y.png: alpha gestript (App Store compliant).
  - Y-dark.png: HSV-luminance ge-invert van Y.png — donkere achtergrond,
    warme cream Y (behoudt brand-hue).
  - Y-tinted.png: hoog-contrast grayscale (light Y op donkere achtergrond)
    voor iOS-tint overlay.
  - Contents.json: dark/tinted entries verwijzen naar de nieuwe files.
  Beide variants mechanisch gegenereerd via Pillow uit Y.png — geen
  design tooling nodig.
- Settings versie-weergave: alleen marketing-versie tonen (was "1.0 (1)",
  nu "1.0"). Build-nummer is een dev-detail dat gebruikers niets zegt.
- Bug-fix: ShareLink toonde mixed-language tekst (NL en EN tegelijk in
  bericht) wanneer systeem-taal en in-app taal verschilden. Oorzaak: het
  `item:` argument gebruikte String(localized:) (systeem-bundle) terwijl
  `subject:`/`message:` Text-LocalizedStringKey gebruiken (in-app override).
  Vervangen door String(format: Bundle.appLocalizedString("Wat vind je van
  de naam %@?"), name). Locaties: FavoriteDetailView.swift:143 en
  ContentView.swift rond regel 700.
- Terminologie-wissel: "draaien"/"spin" → "genereren"/"generate" in
  user-facing strings. Bron-keys hernoemd in xcstrings (NL = key conventie):
  "Draait" → "Genereert", "Nog een keer draaien" → "Nog een keer genereren",
  en de lange overlay-uitleg key (InfoView.swift:32). EN-vertalingen
  meegegaan: Spinning → Generating, Spin again → Generate again, "spin again
  with the magic wand" → "generate again with the magic wand". Interne code
  identifiers (spinReels, isSpinning, spinAgainButton accessibility ID)
  ongewijzigd — die zijn taal-agnostisch.
- Translation review-import van user-bijgewerkte CSV (uit Numbers).
  Inhoudelijk: NL "Pick fullscreen which syllables..." → "Pick which
  syllables..." (typo verwijderd), nieuwe NL voor diezelfde key
  ("Kies welke lettergrepen in de generator mogen terugkomen."), en de
  trailing-space cosmetic op een stale entry (harmloos).
- Scripts/translations.py: detect_delimiter() toegevoegd — Numbers
  exporteert CSV met `;` in EU-locale, Excel met `,`. Script kiest nu
  automatisch o.b.v. de header.
- Bug-fix: bij in-app taalwissel naar EN bleef Settings "Lettertype" tonen
  en bleven de font-namen Nederlands. Twee oorzaken: (1) key "Lettertype"
  in Localizable.xcstrings had een leeg localizations-blok (geen EN-vertaling
  → "Font"); (2) DisplayFont.displayName gebruikte String(localized:) wat
  de runtime BundleLanguage-override negeert. Vervangen door
  Bundle.appLocalizedString conform PROJECT.txt-conventie.
- Volledige NL→EN localisatie afgerond met Localizable.xcstrings; pbxproj
  bijgewerkt met developmentRegion=nl en knownRegions=nl,en,Base (handmatig
  via Xcode UI door user).
- Generator-tuning ronde 1: PowerfulBold uitgebreid (modern niet meer excluded),
  DreamySoft secondaryTags=[romantic,unisex], signatureIDs opgeschoond (geen
  la/mi/ra meer; blair/jax/luz/noe/sve/thor toegevoegd), modernIDs zonder vik,
  accentIDs uitgebreid met ali/rey/rio.
- Scripts/translations.py: CSV export/import voor Localizable.xcstrings — zodat
  vertalingen in Numbers gereviewd kunnen worden. translations.csv staat in
  .gitignore. Import schrijft alleen bij echte wijzigingen.
- Home UI polish: Lettertype-picker is nu een sub-menu Picker (consistent met
  Kleurmodus + Taal), met een gecentreerde live-preview rij eronder.
  Toelichtingen in baseSettings en soundStyle staan nu direct op outer card
  (geen nested blokjes), met 8pt extra top-padding. Badge op Geavanceerd-knop
  is "Auto" / "Handmatig" (was misleidend "Pro").
- Advanced sheet Done-button is nu een handmatige Capsule + .ultraThinMaterial
  pill (matcht de Liquid Glass styling van de syllable picker).
- Paywall refresh: emotionele kop "Vind die ene perfecte naam", 5→3 grouped
  Premium bullets, crown.fill badge, en subtiele "Misschien later" knop onder
  Herstel aankopen.

OPEN PUNTEN (TODO)
------------------
1. App Store last mile:
   - Screenshots in NL én EN voor 6.9", 6.7", 6.1", iPad varianten.
   - App Store Connect: metadata, beschrijving, keywords, leeftijdscategorie,
     privacy URLs (docs/privacy.html via GitHub Pages).
   - Listing copy + promotional text (NL + EN).
   - StoreKit production product matching Configuration.storekit.

2. Push openstaande commits naar origin/main (3 commits ahead).

AFGEROND DEZE WEEK
------------------
- Handmatige device-test door user (OK).
- Translation review (geen verdere wijzigingen nodig).
- App icon polish (light + dark + tinted variants).

BEKENDE CAVEATS
---------------
- pbxproj: developmentRegion=nl, knownRegions=nl,en,Base. Niet zonder akkoord
  aanpassen (build/lokalisatie breekt anders).
- translations.csv staat in .gitignore — gegenereerd bestand.
- Eerste echte import via translations.py geeft eenmalig cosmetische diff
  (lege {} blocks worden compacter); daarna stabiel.
- UI tests gebruiken accessibility identifiers, NIET tekstmatch. Bij refactor
  van die buttons: identifier behouden.
- Codex (parallel agent) commit soms grotere refactors — altijd `git diff`
  bekijken voor commit van zijn werk en samenvattingen toevoegen.
- Liquid Glass toolbar pills renderen automatisch in fullScreenCover ZONDER
  navigationTitle. Met navigationTitle → flat. Workaround = handmatige Capsule.
